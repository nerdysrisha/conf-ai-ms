# ollama-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-nomic-pvc
spec:
  accessModes:
    - ReadWriteOnce  # Multiple pods can access simultaneously
  resources:
    requests:
      storage: 100Gi  # Adjust based on model sizes
  storageClassName: managed-csi-premium